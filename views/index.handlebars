
<h1>Burgers You Have</h1>

<div class="row"><div class="col-6">
<ul>
  {{#each uneatenburgers}}
    <li>
      <button type="button" class="btn btn-primary" id="bite">
        Name: {{burger_name}}
      </button>
    </li>
  {{/each}}
</ul>

</div></div>
<div class="row"><div class="col-6">
<ul>
  {{#each eatenburgers}}
    <li>
      <button type="button" class="btn btn-primary">
        Name: {{burger_name}}
      </button>
    </li>
  {{/each}}
</ul>

</div></div>

<div class="row">
  
<form action="/" method="POST">
	<textarea type="text" name="task"></textarea>

	<button type="submit">Submit</button>
</form>
      </div>









